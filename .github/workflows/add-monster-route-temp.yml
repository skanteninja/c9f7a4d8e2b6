name: Add monster media route temp
on:
  push:
    branches: [main]
    paths: ['.github/workflows/add-monster-route-temp.yml']
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Add monster media rewrite
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path('build.cjs')
          s=p.read_text()
          old="    .replaceAll('https://api.dreamms.gg/api/GMS/latest/character/', '/game-media/characters/')\n"
          new=old+"    .replaceAll('https://api.dreamms.gg/api/GMS/latest/mob/', '/game-media/monsters/')\n"
          assert old in s, 'owned URL insertion target missing'
          if '/game-media/monsters/' not in s:
              s=s.replace(old,new,1)
          p.write_text(s)
          PY
          git rm .github/workflows/add-monster-route-temp.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add build.cjs
          git commit -m 'Route monster renders through owned media path'
          git push
