name: List GMS83 world map IDs
on:
  push:
    branches: [main]
    paths: ['.github/workflows/diag-world-list.yml']
  workflow_dispatch:
jobs:
  list:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsSL --max-time 30 https://maplestory.io/api/GMS/83/map/worldmap
